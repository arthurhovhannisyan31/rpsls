export enum Theme {
  DARK = "DARK",
  LIGHT = "LIGHT"
}

export enum GamePhases {
  IDLE = "IDLE",
  BET = "BET",
  RESULTS = ""
}

export enum NetworkRequestStatus {
  IDLE = "IDLE",
  PENDING = "PENDING",
  DONE = "DONE",
  ERROR = "ERROR"
}

export enum Routes {
  ROOMS = "/rooms",
  ABOUT = "/about"
}
