export enum Actors {
  GUEST = "GUEST",
  HOST = "HOST",
}

export enum UserActions {
  JOINED = "JOINED",
  LEFT = "LEFT",
  PLAYED = "PLAYED"
}

export enum RoundActions {
  START = "START",
  END = "END"
}

export enum Realms {
  ROUND = "ROUND",
}
