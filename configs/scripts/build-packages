#!/bin/bash

. "$(dirname "$0")/configs/colors"

CUR=$(pwd)

for PACKAGE in $(ls ./packages)
do
  cd $CUR/packages/$PACKAGE

  printf "${GREEN}}Run build packages for: ${NC}$PACKAGE \n"

  if ! yarn build
  then
      printf "${RED}}Failed to build: ${NC}$PACKAGE \n"
      exit 1
  fi
done
