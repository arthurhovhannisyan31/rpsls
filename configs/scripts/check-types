#!/bin/bash

export CUR=$(pwd)

for PACKAGE in $(ls ./packages)
do
  cd $CUR/packages/$PACKAGE

  echo "Run check types for: $PACKAGE"

  if ! yarn check-types
  then
      echo "Failed to check types for: $PACKAGE"
      exit 1
  fi
done
